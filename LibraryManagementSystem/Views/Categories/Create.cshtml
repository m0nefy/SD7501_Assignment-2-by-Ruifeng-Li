@model Category
@{ ViewData["Title"] = "Add category"; }<div class="form-shell">
  <p class="eyebrow">CATALOGUE</p>
  <h1>Add a category</h1>
  <form asp-action="Create" class="mt-4">
    <partial name="_Form" model="Model" />
    <button class="btn btn-primary">Save category</button> <a asp-action="Index" class="btn btn-link">Cancel</a>
  </form>
</div>
